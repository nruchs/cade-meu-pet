<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <%- include("partials/header") %>
    </header>
    <%- include("partials/messages") %>
    <main>
        <div class="container">
            <h2>Detalhes do Animal</h2>
        <div class="animal-detalhes animal-card">
            <% if (animal.Foto) { %>
                <img src="<%= animal.Foto %>" alt="<%= animal.Nome %>" width="200" height="200">
            <% } %>
            <p><strong>Nome:</strong> <%= animal.Nome %></p>
            <p><strong>Idade:</strong> <%= animal.Idade %> anos</p>
            <p><strong>Raça:</strong> <%= animal.Raca %></p>
            <p><strong>Características:</strong> <%= animal.Caracteristicas %></p>
            <p><strong>Status:</strong> <%= animal.Status %></p>
            <p><strong>Localização:</strong> <%= animal.Localizacao %></p>
            <a href="/" class="btn">Voltar</a>
        </div>
        <div class="container">
            <h3>Contato do Responsável</h3>
            <p>Nome: <%= usuario.Nome %></p>
            <p>Email: <%= usuario.Email %></p>
            <p>Telefone: <%= usuario.Telefone %></p>
        </div>
    </div>

    </main>

    <script src="/js/script.js"></script>
</body>
</html>
